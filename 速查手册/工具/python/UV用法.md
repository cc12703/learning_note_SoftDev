

# UV用法


## 功能
* 项目脚手架
* 极速包管理
* 虚拟环境管理

## 命令
* `uv init <name> -p <version>` 创建项目

* `uv add <pkg> <pkg>` 添加依赖包
* `uv remove <pkg>` 去除依赖包

* `uv sync`  安装所有依赖，激活虚拟环境
* `uv run <cmd>` 运行脚本
* `uv build`   生成安装包